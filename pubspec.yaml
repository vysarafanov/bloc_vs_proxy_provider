name: bloc_vs_proxy_provider
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  flutter_bloc: ^6.0.5
  provider: ^4.3.2+1
  sealed_flutter_bloc: ^6.0.0
  rxdart: ^0.24.1
  freezed_annotation: ^0.11.0+1
  equatable: ^1.2.5
  json_annotation: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^0.11.5
  build_runner: ^1.10.0
  json_serializable: ^3.4.0

flutter:
  uses-material-design: true

dependency_overrides:
  analyzer: '0.39.14'